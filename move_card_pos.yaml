---
name: move_card_pos
runner_type: "run-python"
description: Move a card to a different position in the same list
enabled: true
entry_point: "move_card_pos.py"
parameters:
  card_id:
    type: string
    description: Card ID to move
    required: true
  target_pos:
    type: string
    description: Target position to move a card to
    required: true
  api_key:
    type: string
    description: User API Key overriding the system key
  token:
    type: string
    description: User oAuth Token overriding the system key

